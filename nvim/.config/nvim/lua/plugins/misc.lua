return {
  -- { -- Allows you to use jj or jk to escape insert mode
  --   -- Disabled because of remapping Capslock to Escape
  --   "max397574/better-escape.nvim",
  --   config = function()
  --     require("better_escape").setup()
  --   end,
  -- },
}
